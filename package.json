{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/auth"
  ],
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "jest --verbose",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "jest": "^24.9.0",
    "lerna": "^3.19.0",
    "ts-jest": "^24.2.0",
    "typescript": "^3.7.3"
  }
}
